<resources>

    <!--
        Base application theme for API 14+. This theme completely replaces
        AppBaseTheme from BOTH res/values/styles.xml and
        res/values-v11/styles.xml on API 14+ devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- API 14 theme customizations can go here. -->
    </style>
    
    <style name="ActionBarBaseStyle" parent="@style/Widget.AppCompat.Light.ActionBar">
        <item name="android:background">@color/actionbar_background</item>
        <item name="android:titleTextStyle">@style/TitleStyle</item>
        <item name="android:icon">@android:color/transparent</item>
        <item name="android:subtitleTextStyle">@style/SubTitle</item>
    </style>

    <style name="ActionBarBaseTheme" parent="@style/Theme.AppCompat.Light">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:actionOverflowButtonStyle">@style/OverflowButton</item>
        <item name="android:actionBarItemBackground">@drawable/actionbar_title_item_background_selector</item>
        <item name="android:actionBarStyle">@style/ActionBarBaseStyle</item>
        <item name="android:dropDownListViewStyle">@style/ListViewDropDown</item>
        <item name="android:itemTextAppearance">@style/OverflowText</item>
    </style>
</resources>
